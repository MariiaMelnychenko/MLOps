# Resolvable deps for the ML training image (Dockerfile). Avoids dvc vs black pathspec conflict in the full freeze.
pandas>=2.0,<3
numpy>=1.24,<3
scikit-learn>=1.3,<2
matplotlib>=3.7,<4
seaborn>=0.13,<1
mlflow>=2.10,<4
pyarrow>=14
pyyaml>=6
joblib>=1.3
dvc>=3.0,<4
